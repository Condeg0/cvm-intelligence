# Full development requirements — for running the complete pipeline locally.
# Requires a GPU (RTX A1000 or better) and 16GB+ RAM.
# For Streamlit Cloud deployment, use requirements.txt instead.

# Core ML
torch>=2.1.0
transformers>=4.36.0
sentence-transformers>=2.2.2

# PDF Processing
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# Vector Store & Retrieval
chromadb>=0.4.0
rank-bm25>=0.2.2

# NLP & ML
datasets>=2.14.0
scikit-learn>=1.3.0
joblib>=1.3.0

# API — Gemini 2.5 Flash (new SDK; google-generativeai kept as fallback)
google-genai>=1.0.0
google-generativeai>=0.8.0

# Application
streamlit>=1.28.0
plotly>=5.18.0
altair>=5.0.0

# Data
pandas>=2.1.0
numpy>=1.24.0

# Utilities
tqdm>=4.66.0
python-dotenv>=1.0.0

# Testing
pytest>=7.4.0
tabulate>=0.9.0     # required by pandas DataFrame.to_markdown() in notebooks

# Database (sqlite3 is stdlib — no install needed)
