# Streamlit Cloud runtime dependencies.
# Only what the four app pages need — no GPU, no ChromaDB, no PyMuPDF.
# Heavy pipeline deps live in requirements-dev.txt.

# Application
streamlit>=1.28.0
plotly>=5.18.0
altair>=5.0.0

# Data
pandas>=2.1.0
numpy>=1.24.0

# API — Gemini 2.5 Flash (RAG generation when running locally with ChromaDB)
google-genai>=1.0.0
google-generativeai>=0.8.0

# HuggingFace Hub — model card links and optional ChromaDB snapshot download
huggingface_hub>=0.21.0

# Utilities
python-dotenv>=1.0.0
