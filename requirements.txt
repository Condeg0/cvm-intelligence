# Streamlit Cloud deployment requirements.
# Contains only what the four app pages need at runtime.
# Heavy pipeline deps (torch, chromadb, PyMuPDF, etc.) live in requirements-dev.txt.

# Application
streamlit>=1.28.0
plotly>=5.18.0
altair>=5.0.0

# Data
pandas>=2.1.0
numpy>=1.24.0

# API — Gemini 2.5 Flash (used by the RAG query page when running locally with data)
google-genai>=1.0.0
google-generativeai>=0.8.0

# Utilities
python-dotenv>=1.0.0
